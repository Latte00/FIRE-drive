hydra:
  run:
    dir: ${output_dir}
  output_subdir: ${output_dir}/code/hydra           # Store hydra's config breakdown here for debugging
  searchpath:                                       # Only <exp_dir> in these paths are discoverable
    - pkg://navsim.planning.script.config.common
  job:
    chdir: False

defaults:
  - default_common
  - default_evaluation
  - default_train_val_test_log_split
  - agent: ego_status_mlp_agent
  - _self_

split: trainval
cache_path: ${oc.env:NAVSIM_EXP_ROOT}/training_cache
use_cache_without_dataset: True # load the training samples from the cache. scene-filter will be ignored
force_cache_computation: true
scorer_cache_path: null
scorer_cache_filename: scorer_cache.gz
scorer_cache_require: false
scorer_cache_use_train_mode: false
scorer_cache_force_train_forward: false
seed: 0
# 更换监控数据集
monitor_enable: false  # add a second validation loader used only for monitoring/checkpoint selection
monitor_use_test_logs: true  # when monitor_logs is empty, fallback to test_logs from split config
monitor_logs: []  # explicit monitor log names (overrides monitor_use_test_logs)
monitor_schedule:
  enable: false  # adapt validation/monitor frequency by epoch
  switch_epoch: 50  # 1-based epoch where interval switches to every_n_epoch_after
  every_n_epoch_before: 1  # check validation every N epochs before switch_epoch
  every_n_epoch_after: 1  # check validation every N epochs at/after switch_epoch

dataloader:
  params:
    batch_size: 64  # number of samples per batch
    num_workers: 4  # number of workers for data loading
    pin_memory: False  # pin memory for faster GPU transfer
    prefetch_factor: 2  # number of samples loaded in advance by each worker

trainer:
  params:
    max_epochs: 100  # maximum number of training epochs
    check_val_every_n_epoch: 1  # run validation set every n training epochs
    val_check_interval: 1.0  # [%] run validation set every X% of training set

    limit_train_batches: 1.0  # how much of training dataset to check (float = fraction, int = num_batches)
    limit_val_batches: 1.0  # how much of validation dataset to check (float = fraction, int = num_batches)

    accelerator: gpu  # distribution method
    strategy: ddp_find_unused_parameters_true  # auto select strategy
    precision: 16-mixed  # floating point precision
    num_nodes: 1  # Number of nodes used for training

    num_sanity_val_steps: 0  # number of validation steps to run before training begins
    fast_dev_run: false  # runs 1 batch of train/val/test for sanity

    accumulate_grad_batches: 1  # accumulates gradients every n batches
    # track_grad_norm: -1  # logs the p-norm for inspection
    gradient_clip_val: 2.0  # value to clip gradients
    gradient_clip_algorithm: norm  # [value, norm] method to clip gradients
    default_root_dir: ${output_dir}
    ckpt_path: #/home/xqf/DiffusionDrive-main/exp/training_diffusiondrive_agent/2026.01.07.19.46.36/lightning_logs/version_0/checkpoints/epoch=49-step=66500.ckpt
